#ifndef MINE_CHOOSE_HPP
#define MINE_CHOOSE_HPP

#include <graphics.h>
#include <easyx.h>

using namespace std;

void button(int x, int y, int w, int h, const string& text);
int choose(int x, int y, bool flag);

#endif //MINE_CHOOSE_HPP
